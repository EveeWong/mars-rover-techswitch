import React from 'react';
import { render } from '@testing-library/react';
import { RoverSelection } from './RoverSelection';

test('renders the RoverSelection component without errors', () => {
  render(<RoverSelection/>);
});